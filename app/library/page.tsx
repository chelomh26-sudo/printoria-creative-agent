"use client";
/* eslint-disable @next/next/no-img-element -- signed private URLs expire and should not pass through the image optimizer */

import { ChangeEvent, FormEvent, useCallback, useEffect, useState } from "react";
import { WorkspaceShell } from "@/components/WorkspaceShell";

type Asset = { id: string; name: string; mime_type: string; asset_class: "reference" | "locked"; category: string; preview_url: string | null; created_at: string };

export default function LibraryPage() {
  const [assets, setAssets] = useState<Asset[]>([]); const [file, setFile] = useState<File | null>(null); const [assetClass, setAssetClass] = useState("reference"); const [category, setCategory] = useState("visual_reference"); const [notice, setNotice] = useState(""); const [busy, setBusy] = useState(false);
  const load = useCallback(() => fetch("/api/library").then((response) => response.json()).then((result) => { if (result.error) throw new Error(result.error); setAssets(result.assets); }).catch((error) => setNotice(error.message)), []);
  useEffect(() => { load(); }, [load]);
  async function upload(event: FormEvent) { event.preventDefault(); if (!file) return; setBusy(true); setNotice(""); const form = new FormData(); form.set("file", file); form.set("assetClass", assetClass); form.set("category", category); const response = await fetch("/api/library", { method: "POST", body: form }); const result = await response.json(); setBusy(false); if (!response.ok) setNotice(result.error); else { setFile(null); setNotice("Archivo guardado en la biblioteca."); await load(); } }
  async function remove(id: string) { if (!confirm("¿Eliminar este archivo de la biblioteca?")) return; await fetch(`/api/library?id=${id}`, { method: "DELETE" }); await load(); }
  return <WorkspaceShell active="library" subtitle="Assets permanentes de Printoria. Define si inspiran o si deben preservarse exactamente." title="Biblioteca de marca"><form className="library-uploader" onSubmit={upload}><label className="library-file"><span>↑</span><strong>{file?.name ?? "Seleccionar archivo"}</strong><input onChange={(event: ChangeEvent<HTMLInputElement>) => setFile(event.target.files?.[0] ?? null)} type="file" /></label><label>Clasificación<select onChange={(event) => setAssetClass(event.target.value)} value={assetClass}><option value="reference">REFERENCE · inspira</option><option value="locked">LOCKED · preservar</option></select></label><label>Categoría<select onChange={(event) => setCategory(event.target.value)} value={category}><option value="logo">Logo</option><option value="mascot">Mascota</option><option value="product">Producto</option><option value="order">Pedido real</option><option value="visual_reference">Referencia visual</option><option value="document">Documento</option><option value="other">Otro</option></select></label><button className="primary-button" disabled={!file || busy} type="submit">{busy ? "Subiendo…" : "Guardar asset"}</button></form>{notice && <p className="form-notice">{notice}</p>}<div className="asset-grid">{assets.map((asset) => <article className="brand-asset-card" key={asset.id}>{asset.preview_url && asset.mime_type?.startsWith("image/") ? <img alt={asset.name} src={asset.preview_url} /> : <div className="document-preview">DOC</div>}<div><span className={`asset-badge ${asset.asset_class}`}>{asset.asset_class.toUpperCase()}</span><h3>{asset.name}</h3><p>{asset.category}</p></div><button onClick={() => remove(asset.id)} type="button">Eliminar</button></article>)}</div></WorkspaceShell>;
}
